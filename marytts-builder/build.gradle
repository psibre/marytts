dependencies {
    compile(rootProject.allprojects.find { it.name == 'marytts-client' }) {
        transitive = true
    }
    testCompile diff4j
    testCompile festassert
    testCompile hamcrest
    testCompile junit
    compile mwdumper
    compile oro
    compile sgt
    compile wekaDev
}
